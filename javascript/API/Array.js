/*
* Array.concat
* 数组衔接
* @param 多值，多数组，浅拷贝
* @return 新数组
* */

/*
* Array.every
* 测试断言函数是否对每个元素为真
* array.every(predicate, o)
* @param predicate 断言函数
* @param o         调用predicate的可选this值
* @return 如果对于array每个元素predicate都返回true,则返回true,否则停止遍历,返回false
*
* 空数组时返回true
*
* 调用predicate带有的三个参数
* predicate(array[i], i, array)
* */
